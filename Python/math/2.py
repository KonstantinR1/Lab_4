# Get input from the user
height = float(input("Height: "))
base1 = float(input("Base_1: "))
base2 = float(input("Base_2: "))

# Calculate the area of the trapezoid
area = 0.5 * (base1 + base2) * height

# Print the result
print(f"Expected Output: {area}")